<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('homepage') }}">AddressBook</a>
        <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#menu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="menu">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('app_contact_list') }}" data-bs-toggle="dropdown">
                        Contacts
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_contact_list') }}">List</a></li>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li><a class="dropdown-item" href="{{ path('app_contact_add') }}">Add</a></li>
                        {% endif %}
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="{{ path('app_company_list') }}" data-bs-toggle="dropdown">
                        Companies
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_company_list') }}">List</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="nav-link" href="{{ path('app_login') }}">
                            Login
                        </a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            Logout {{ app.user.username }}
                        </a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
</nav>

